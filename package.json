{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.3"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.{js,json}": [
      "prettier --write"
    ],
    "frontend/src/**/*.{js,jsx,json,css}": [
      "prettier --write"
    ]
  }
}
